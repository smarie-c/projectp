<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

<header>
<div class="small-12 collumns">
    <% if user_signed_in? %>
      <%= link_to current_user.email, edit_user_registration_path, :class => "login" %>
    <% end %>
  <div class="title">Nexus</div>

</div>
  
</header>



<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

<p class="navbar-text pull-right">
<!--<% if user_signed_in? %>
<%= link_to current_user.email, edit_user_registration_path, :class => "button [radius round]"  %>
<%= link_to("Logout", destroy_user_session_path, method: :delete, :class => "btn btn-large ") %>
  <%= link_to t('tournament_list'), tournoi_index_path, :class => 'btn btn-large btn-success'  %>
  <%= link_to t('tournament_create'), tournoi_create_view_path, :class => 'btn btn-large btn-info'  %>
  <%= link_to t('home'), root_path, :class => 'btn btn-small btn-warning' %>
<% else %>
  <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-small btn-success'  %> |
  <%= link_to "Login", new_user_session_path, :class => 'btn btn-small btn-info'  %>
<% end %> -->
<div class="container">

<%= yield %>

      <p class="navbar-text pull-right">
        <% if user_signed_in? %>

        <% end %>
      </p>








  </body>
</html>