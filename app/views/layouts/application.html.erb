<!DOCTYPE html>
<html>
<head>
  <title>Projetp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

<p class="navbar-text pull-right">
<% if user_signed_in? %>
  Logged in as <strong><%= current_user.email %></strong>.
          <%= link_to t('tournament_list'), tournoi_index_path, :class => 'btn btn-large btn-success'  %>
        <%= link_to t('tournament_create'), tournoi_create_view_path, :class => 'btn btn-large btn-info'  %>
  <%= link_to t('home'), root_path, :class => 'btn btn-small btn-warning' %>
  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'btn btn-small btn-primary' %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-small btn-danger'  %>
<% else %>
  <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-small btn-success'  %> |
  <%= link_to "Login", new_user_session_path, :class => 'btn btn-small btn-info'  %>
<% end %>
<div class="container">

<%= yield %>

      <p class="navbar-text pull-right">
        <% if user_signed_in? %>

        <% end %>
      </p>
</div>
</body>
</html>
